GHL_CLIENT_ID=
GHL_CLIENT_SECRET=
# Must match a redirect URI registered on the GHL marketplace app.
GHL_REDIRECT_URI=http://localhost:8734/oauth/callback
# Space-separated OAuth scopes requested during `npm run authorize`.
GHL_SCOPES=contacts.readonly opportunities.readonly calendars/events.readonly
